services:
  mondodb:
    image: mongodb:v1
    container_name: mongodb
  catalouge:
    image: catalouge:v1
    container_name: catalouge

networks:
  default:
    driver: bridge
    name: roboshop
    external: true